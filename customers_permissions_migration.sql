-- ══════════════════════════════════════════════════════════════════════
-- 👥 CUSTOMER PERMISSIONS MIGRATION
-- صيدلية العصافرة — v13
-- يضيف عمود permissions لجدول users لتحكم الأدمن في وصول كل موظف
-- ══════════════════════════════════════════════════════════════════════

-- 1. إضافة عمود permissions لجدول users
ALTER TABLE users
ADD COLUMN IF NOT EXISTS permissions JSONB DEFAULT '{}' NOT NULL;

-- 2. تعليق توضيحي على العمود
COMMENT ON COLUMN users.permissions IS
  'صلاحيات المستخدم على مستوى الميزات — مثال: {"customers": true}';

-- ══════════════════════════════════════════════════════════════════════
-- مفاتيح الصلاحيات المستخدمة في التطبيق:
--   "customers"  → وصول تاب متابعة العملاء + دليل العملاء
-- ══════════════════════════════════════════════════════════════════════

-- للتحقق من التطبيق الناجح:
-- SELECT id, name, role, permissions FROM users;
