# دليل تسجيل عميل جديد — فارما فلاي SaaS

## المتطلبات (مرة واحدة فقط — تُعمل مرة وحدة في حياتك)

1. شغّل `pharmafly_setup.sql` في Supabase بتاعك (SQL Editor → Run)
2. انشر `pharmafly-template.html` على Vercel — **رابط واحد لكل العملاء**

---

## لكل عميل جديد (10 دقائق)

### الخطوة 1 — Firebase للعميل

1. افتح [console.firebase.google.com](https://console.firebase.google.com)
2. **Add project** ← اسم الصيدلية ← Skip Analytics ← **Create**
3. **Authentication → Get started → Email/Password → Enable → Save**
4. **Project Settings → Your apps → </> → Register app**
5. انسخ الـ firebaseConfig وابعته للعميل

---

### الخطوة 2 — العميل يسجّل نفسه

1. العميل يفتح رابط Vercel
2. يُدخل: اسم الصيدلية + Firebase config
3. يضغط "حفظ والبدء"
4. ينشئ حساب المدير — أول حساب = admin تلقائياً
5. بياناته منفصلة 100% عن باقي العملاء

النظام ينشئ pharmacy_id فريد تلقائياً — لا تدخل يدوي منك.

---

### الخطوة 3 — Authorized Domain في Firebase

Authentication → Settings → Authorized domains → Add domain
← أضف رابط Vercel بتاعك (مرة واحدة بس لكل عملاء)

---

## إدارة الاشتراكات

كل العملاء في Supabase بتاعك في جدول pharmacies:

```sql
-- تفعيل عميل بعد الدفع
UPDATE pharmacies SET plan = 'active' WHERE id = 'uuid-هنا';

-- إيقاف عميل
UPDATE pharmacies SET plan = 'suspended' WHERE id = 'uuid-هنا';

-- تمديد تجربة
UPDATE pharmacies SET trial_ends = NOW() + INTERVAL '30 days' WHERE id = 'uuid-هنا';

-- عرض كل العملاء
SELECT id, name, plan, trial_ends, created_at FROM pharmacies ORDER BY created_at DESC;
```

عند suspended أو انتهاء التجربة → شاشة "انتهى الاشتراك" تظهر للعميل.

---

## تسعيرة مقترحة

| الخدمة | السعر |
|--------|-------|
| تجربة مجانية | 30 يوم |
| اشتراك شهري | 300-500 جنيه |
| اشتراك سنوي | 2500-4000 جنيه |

---

## ملاحظات

- Supabase free tier يكفي 10-15 صيدلية
- عند تجاوز 15 صيدلية: Supabase Pro = $25/شهر
- تحديثات البرنامج: عدّل pharmafly-template.html وأعد النشر — كل العملاء يحصلون على التحديث فوراً
