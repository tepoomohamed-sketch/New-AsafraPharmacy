# صيدلية العصافرة - النسخة الجديدة المؤقتة

## 📋 نظرة عامة

هذه نسخة **مستقلة تمامًا** من تطبيق صيدلية العصافرة، مربوطة بمشروع Supabase جديد مؤقت لحين عودة المشروع القديم للعمل.

- **الملف الرئيسي**: `صيدلية العصافرة - جديد.html`
- **ملف SQL**: `setup_asafra_new.sql`
- **المشروع**: `evqirssbauneitjpztbq`

---

## ⚙️ خطوات التشغيل (إلزامية)

### 1️⃣ تشغيل SQL في Supabase

قبل فتح التطبيق، شغّل ملف `setup_asafra_new.sql` **مرة واحدة فقط** في المشروع الجديد:

1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. اختر مشروع `evqirssbauneitjpztbq`
3. من القائمة الجانبية: **SQL Editor**
4. اضغط **New Query**
5. انسخ محتوى ملف `setup_asafra_new.sql` كاملاً
6. اضغط **Run** أو `Ctrl+Enter`
7. تأكد من ظهور رسالة نجاح، ثم من نتيجة الاستعلام النهائي أن الجداول الخمسة موجودة:
   ```
   attendance
   entries
   entry_actions
   meta
   users
   ```

**ملاحظة**: لو شغّلت الملف أكثر من مرة، لن يحدث ضرر لأنه يستخدم `IF NOT EXISTS` و`CREATE OR REPLACE`.

---

### 2️⃣ فتح التطبيق

1. افتح ملف `صيدلية العصافرة - جديد.html` في متصفح Chrome أو Edge.
2. ستظهر شاشة تسجيل الدخول.

---

### 3️⃣ أول دخول (إنشاء المدير تلقائيًا)

- عند **أول** دخول ناجح من أي حساب Firebase:
  - إذا كان جدول `users` في Supabase **فارغًا تمامًا**، التطبيق سيُنشئ سجلاً للمستخدم تلقائيًا بدور `admin`.
  - بعد ذلك يتم فتح التطبيق مباشرة.

- **الحسابات التالية**: أي حساب Firebase آخر يدخل ولا يملك سجلاً في جدول `users` سيتم منعه، ويجب على المدير إضافته يدويًا من تبويب **المستخدمون**.

---

## 🔐 تسجيل الدخول

التطبيق يستخدم **Firebase Authentication** الحالي، لذلك:

- البريد الإلكتروني وكلمة المرور **نفسها** من النسخة القديمة تعمل.
- لو لم يكن عندك حساب Firebase بعد، اضغط **حساب جديد** من شاشة الدخول.

---

## ✅ ماذا تم تغييره؟

| العنصر | القديم | الجديد |
|---|---|---|
| **Supabase URL** | `pjrmwnntmkrkemwhqppw` | `evqirssbauneitjpztbq` |
| **Supabase Key** | مفتاح قديم | المفتاح المرسل الجديد |
| **POS localStorage** | `asafra_pos_v1` | `asafra_new_pos_v1` |
| **Backup IndexedDB** | `asafra_bkp_v1` | `asafra_new_bkp_v1` |
| **كل مفاتيح التخزين** | بدون مقدمة | `asafra_new_*` |
| **جدول pos_sales** | موجود في Supabase | محذوف، POS محلي فقط |
| **البيانات المنقولة** | لا شيء | النسخة تبدأ فارغة |

---

## 📦 ماذا يحتوي المشروع الجديد؟

الجداول المُنشأة في Supabase:

### `entries` — القيود المالية
- كل الأعمدة الأساسية + الحذف المرن + مراجعة POS
- الرصيد يبدأ من **صفر**

### `attendance` — الحضور والانصراف
- جاهز مع عمود `notes` لتسجيل ملاحظات المشرف

### `users` — حسابات المستخدمين
- يحتوي على دور المستخدم والصلاحيات
- يربط Firebase UID بـ Supabase

### `meta` — الإعدادات العامة
- الرصيد، الأهداف، الإعدادات المخصصة، إلخ
- تبدأ فارغة

### `entry_actions` — سجل حركات المدير
- تسجيل الحذف، التسوية، تعليم POS
- إمكانية التراجع عن كل حركة

---

## ⚠️ أمور مهمة

### ✅ النسخة القديمة لم تُمس
- ملف `صيدلية العصافرة -v13.html` الأصلي **لم يتغير**.
- بيانات POS القديمة في `localStorage` تحت مفتاح `asafra_pos_v1` **لم تُمسح**.
- عند فتح الملف القديم، ستجد كل شيء كما هو.

### 🔄 النسخة الجديدة مستقلة تمامًا
- POS يبدأ فارغًا (استورد ملف Excel جديد).
- القيود والحضور والإعدادات تبدأ فارغة.
- الرصيد الافتتاحي = **صفر**.

### 🚫 POS لا يُرفع إلى Supabase
- بيانات POS تُحفظ في `localStorage` فقط (`asafra_new_pos_v1`).
- عند تصدير Excel أو نسخ احتياطي، POS يُضمّن محليًا فقط.
- **مهم**: إذا مسحت بيانات المتصفح، ستفقد POS المحلي.

### 🔑 Firebase مشترك
- النسختان (القديمة والجديدة) تستخدمان نفس Firebase.
- لذلك تسجيل الدخول قد يكون محفوظًا من الجلسة السابقة.
- الفرق: صلاحيات المستخدم وبياناته تُحدد من جدول `users` في Supabase المناسب لكل نسخة.

---

## 📝 خطوات ما بعد التشغيل

### 1. إضافة موظفين
من تبويب **المستخدمون**، المدير يستطيع:
- إضافة موظف جديد من Firebase.
- تحديد الدور: موظف أو مدير.
- تعيين الصلاحيات (إذا كان الموظف يحتاج صلاحيات خاصة).

### 2. إدخال الرصيد الافتتاحي
من تبويب **الملخص**:
- اضغط **تعديل الرصيد** واختر **رصيد افتتاحي**.
- أدخل الرصيد الأولي لبدء الحسابات.

### 3. إضافة قيود مالية
من تبويب **كل الحركات** أو **حركاتي**:
- اضغط **قيد جديد**.
- اختر النوع (إيرادات / مصاريف / طلبيات / ذمم أونلاين / إلخ).
- أدخل البيانات واحفظ.

### 4. استيراد POS
من تبويب **مقارنة POS**:
- اضغط **استورد POS**.
- اختر ملف Excel من نظام POS الحالي.
- البيانات تُحفظ محليًا فقط.

---

## 🔧 استكشاف الأخطاء

### ❌ "البريد أو كلمة المرور خاطئ"
- تأكد أن الحساب موجود في Firebase.
- جرّب إنشاء حساب جديد من **حساب جديد**.

### ❌ "حسابك غير مفعّل — تواصل مع المدير"
- هذا يعني أن حساب Firebase موجود، لكن لا يوجد سجل في جدول `users`.
- الحل: المدير يضيف المستخدم يدويًا من تبويب **المستخدمون**.

### ❌ الصفحة لا تُحمّل أو تظهر أخطاء console
- تأكد أن ملف `setup_asafra_new.sql` تم تشغيله بنجاح.
- افتح **DevTools** (`F12`) → **Console** وابحث عن أخطاء.
- تأكد أن المشروع `evqirssbauneitjpztbq` **ليس عليه تقييد Egress**.

### ⚠️ POS اختفى
- POS محلي فقط ويُحفظ في `localStorage`.
- إذا مسحت بيانات المتصفح أو فتحت النسخة من جهاز آخر، POS لن يظهر.
- الحل: استورد ملف Excel من جديد.

---

## 📂 الملفات المسلّمة

```
D:\new\asafra pharmacy\
├── صيدلية العصافرة - جديد.html    ← التطبيق الجديد
├── setup_asafra_new.sql            ← SQL للمشروع الجديد
├── تعليمات - النسخة الجديدة.md     ← هذا الملف
└── صيدلية العصافرة -v13.html      ← النسخة القديمة (لم تُمس)
```

---

## 🎯 الخلاصة

- النسخة الجديدة **برنامج مستقل** مربوط بمشروع Supabase مؤقت.
- تبدأ **بداتا فارغة** (لا تنقل أي شيء من القديم).
- أول دخول Firebase **يُنشئ المدير تلقائيًا** إذا كان جدول `users` فارغ.
- POS **محلي فقط**، لا يُرفع إلى Supabase.
- النسخة القديمة وبياناتها **لم تُمس**.

---

✅ **جاهز للتشغيل!**

شغّل SQL أولاً، ثم افتح الملف واستمتع بالنسخة الجديدة المستقلة.